// Jenkins encodes backslashes, so we need to have this string in case the branch has a backslash in it, otherwise it would fail
//def jenkinsProjectName = env.BRANCH_NAME.replaceAll('[/.]', '%2F')

build job: "scripting-pipelines-code-references/main", propagate: false, wait: false

node {

    stage('Build') {

    }

    stage('Test') {

    }

    stage('Deploy') {

    }
}